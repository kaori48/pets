<div class="row">
  <div class="login col-xs-4 col-xs-offset-4">
  <h3>ログイン</h3><br>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email,:size=>"50x1", autofocus: true, autocomplete: "email" %>
  </div><br>

  <div class="field">
    <%= f.label :password %><br/>
    <%= f.password_field :password,:size=>"50x1", autocomplete: "current-password" %>
  </div><br>

  <% if devise_mapping.rememberable? %>
  <div class="field">
    <%= f.check_box :remember_me %>
    <%= f.label :remember_me %>
  </div><br>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン",class: "btn btn-warning btn-block" %>
  </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
</div>